[project]
name = "cookieclicker_stockmarket_sim"
version = "0.1.0"
description = "Cookie Clicker Stock Market Simulation Engine"
dependencies = [
    "gymnasium",
    "numpy",
    "pandas",
    "matplotlib"
]

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
